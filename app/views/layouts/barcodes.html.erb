<!DOCTYPE html>
<html>
  <head>
    <title>NCAS Equipment - Barcode Scanner</title>
    <meta name="viewport" content="width=device-width,initial-scale=1,user-scalable=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= action_cable_meta_tag %>

    <%= stylesheet_pack_tag 'barcode-scanner', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'barcode-scanner', media: 'all', 'data-turbolinks-track': 'reload' %>

    <script type="text/javascript">
      window.BarcodeApp = {
        action_types: <%= raw(BarcodeUtils.action_types.to_json) %>,
        message_types: <%= raw(BarcodeUtils.message_types.to_json) %>
      };
    </script>
  </head>

  <body>
    <%= yield %>
    <%= render "javascript" %>
  </body>
</html>
